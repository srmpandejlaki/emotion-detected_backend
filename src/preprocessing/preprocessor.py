class Preprocessor:
    def preprocess(self, data):
        """ Harus diimplementasikan oleh child class """
        raise NotImplementedError(
            "Subclasses must implement `preprocess` method")
